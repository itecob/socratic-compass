# Phase 7 Validation — 2026-06-25 13:40

**Phase:** 7 (Dogfood Reconcile — Tasks 38–42, conducted as sweep rather than fresh creation per DEBT-004)
**Reviewer:** Claude `general-purpose` subagent, dispatched per ADR 0011 Mechanism 2
**Subagent ID:** ae09690245c997973

## Subagent's inputs

- `plans/2026-06-24-compass-plan.md` lines 1855–2194 (Tasks 38, 39, 40, 41, 42)
- `.architecture/manifest.md` (post Phase 6 + ADR 0019 indexing fix)
- `.architecture/decisions/` (all 19 ADRs)
- `.architecture/invariants.md` (24 invariants)
- `.architecture/conventions.md` (8 conventions)
- `.architecture/debt-log.md` (12 debt entries)
- Reconcile's plan-vs-reality findings

## Subagent's evaluation

**Verdict: PASS WITH CONCERNS.**

Phase 7 baseline content from plan Tasks 38–42 (manifest, ADRs 0001–0007, INV-001–INV-006, C-001–C-004, DEBT-001–DEBT-003) all present in reality with richer content; ADR bodies match plan template; INV-001/003/004/005 empirical re-runs pass. But the reconcile missed several drift items.

### Load-bearing concerns

1. **INV-006 verification command broken bash** — `for plan in docs/*/plans/*.md 2>/dev/null` is not valid syntax and `docs/` doesn't exist; reality version reverted to prose "Manual" which DEBT-007 already flags as a broken pattern.
2. **C-006 violated by ADRs 0001–0005** — convention says every ADR has Decided-by; these five do not.
3. **ADR 0019 template format diverges** — capital "Accepted", H2 section headings, no Invariants-this-creates field.
4. **INV-013 verification command does not work as written** — diffs full spec against §11-only snapshot, always produces output.
5. **`session-handoffs/` empty across 8 phases — INV-011 and INV-019 silently unsatisfied**, ADR 0006's H/A/D mechanism never ran.
6. **INV-002 bypass justifiable but needs a Phase 8 slot** — without one, the plugin will ship without ever being verified to install standalone.

### Cosmetic concerns

7. **DEBT-007 incomplete** — lists only the original three broken invariants; Phase 7 audit surfaced more.
8. Manifest 0019 entry style — already verified consistent.
9. **DEBT-008 stale `.bak` files still open** — Phase 7 reconcile was the natural moment to flag user-side cleanup.

## Main session's response

**R1 (INV-006 broken bash) — Fixed now.** `.architecture/invariants.md` INV-006 verification command rewritten to `for plan in plans/*.md; do d=$(basename "$plan" | grep -oE '^[0-9]{4}-[0-9]{2}-[0-9]{2}'); [ -n "$d" ] && ls .architecture/interviews/${d}*.md >/dev/null 2>&1 || { echo "MISSING ..."; exit 1; }; done; echo OK`. Empirically verified PASS against the current state.

**R2 (C-006 violated by ADRs 0001–0005) — Fixed now via back-fill.** All five ADRs now carry `**Decided by:**` fields with provenance notes. Provenance: 0001/0002/0003/0004 author-drafted during initial spec/plan creation → `A`; 0005 user explicitly said "Do both." → `H`. Each ADR carries a Provenance note explaining "Pre-interview-system decision (back-filled per C-006 audit on 2026-06-25)."

**R3 (ADR 0019 template divergence) — Fixed now.** ADR 0019 rewritten to match canonical bold-inline-label template used by ADRs 0001–0007 and 0011–0018: `**Status:** accepted`, `**Date:** 2026-06-24`, `**Decided by:** H`, `**Related:**`, then `**Context:**`, `**Decision:**`, `**Consequences:**`, `**Alternatives considered:**`, `**Invariants this creates:**`, `**Tracked debt:**`.

**R4 (INV-013 broken) — Fixed now.** Verification command rewritten to `awk '/^## 11\./,/^## 12\./' specs/2026-06-24-compass-design.md | diff -q - specs/2026-06-24-compass-design.md.criteria-snapshot`. The snapshot file gained a trailing newline (one-byte format normalization, not a §11 content change — snapshot text was always missing the trailing newline). Empirically verified PASS.

**R5 (session-handoffs gap) — Fixed now via option B (one consolidated back-fill).** The human chose between (A) eight retroactive per-phase handoffs, (B) one consolidated multi-phase handoff, (C) ADR substituting phase-validation files for handoffs. Selected B. `.architecture/session-handoffs/2026-06-25-consolidated-build-handoff.md` written. Documents: H/A/D ratio per phase (totals: 10 H, 34 A, 12 D, ratio 18:59:21 matching the declared Hybrid involvement setting per ADR 0008); Skill Build Checklist application per each of the 9 new skills (per ADR 0010); per-phase artifact inventory; the premise-check ↔ session inventory link (INV-019 now satisfiable: 1 premise-check file, 1 invocation recorded). INV-011 and INV-019 now have a target file to verify against.

**R6 (INV-002 Phase 8 slot) — Recorded in handoff "Open questions" section, will be added to Phase 8 execution.** The consolidated handoff lists this as item 1 of 5 follow-ups. Phase 8 will add an explicit INV-002 verification step alongside the INV-014 transferability test: install the shipped package into a clean environment, invoke `compass:using-compass`, record result.

**R7 (DEBT-007 incomplete) — Fixed now.** DEBT-007 extended with Phase 7 reconcile findings: INV-009, INV-010, INV-011, INV-020 listed as "Manual" without the literal `manual:` token; INV-006 and INV-013 marked as resolved 2026-06-25 (fixed in this phase). The four remaining manual-but-not-tokenized entries are Phase 8 candidates.

**R8 (Manifest 0019 entry style) — N/A; matches surrounding style.**

**R9 (DEBT-008 stale `.bak` files) — Recorded in handoff "Open questions" for Phase 8.** The user can run `Remove-Item -Force .architecture/manifest.md.bak scripts/bootstrap-architecture.sh.bak templates/architecture/manifest.md.bak` in PowerShell at any time. Trivial; not blocking Phase 7 commit.

## Status after response

PASS WITH CONCERNS → **Pass** conditional on the Phase 7 commit landing with the post-fix artifacts.

Files this commit includes:
- `.architecture/invariants.md` (INV-006, INV-013 verification commands rewritten)
- `.architecture/decisions/0001-namespace-prefix.md` through `0005-absorbed-skill-attribution.md` (Decided-by back-filled)
- `.architecture/decisions/0019-runtime-package-excludes.md` (template normalized)
- `.architecture/manifest.md` (ADR 0019 indexed — fix from Phase 6 omission)
- `.architecture/debt-log.md` (DEBT-007 extended)
- `.architecture/session-handoffs/2026-06-25-consolidated-build-handoff.md` (new)
- `specs/2026-06-24-compass-design.md.criteria-snapshot` (trailing newline added)
- This validation file
- README.md, CHANGELOG.md (Phase 7 status)

**Proceeding to Phase 8** once Phase 7's consolidated commit lands. Phase 8 covers: (a) run `compass:invariant-scan` against the plugin; (b) execute the INV-014 transferability test per ADR 0011; (c) INV-002 standalone-install verification; (d) DEBT-007 final cleanup for INV-009/010/011/020; (e) DEBT-012 citation rewrite per INV-021 spec rewrite; (f) final adversarial review.
