# Phase 8 Validation — 2026-06-25 15:30

**Phase:** 8 (Final verification / ship gate — Tasks 43–45)
**Reviewer:** Claude `general-purpose` subagent, dispatched per ADR 0011 Mechanism 2
**Subagent ID:** ad86f2092abc9ee97

## Subagent's inputs

- Final invariant scan output (19 PASS, 4 MANUAL, 1 INFO, 0 FAIL)
- All 24 invariants in `.architecture/invariants.md`
- All 19 ADRs
- 13 debt entries
- `.architecture/validation/transferability-2026-06-25.md` (INV-014 result, synthetic methodology)
- `.architecture/session-handoffs/2026-06-25-consolidated-build-handoff.md`
- Rewritten spec sections (§3.3, §3.6)
- `dist/compass.plugin` and `dist/compass-claude-code/` (sandbox-side state)
- All 23 SKILL.md files, 4 hook scripts, 2 packaging scripts, LICENSE, NOTICE, plugin.json

## Subagent's evaluation

**Verdict: HOLD.**

The reviewer flagged 4 ship-blockers and 7 secondary concerns. The discipline is real; the build is largely sound; but several items contradict the v0.1.0 ship-claim as-of-review-time.

### Ship-blockers identified

1. **`dist/compass.plugin` on disk was a corrupted/truncated zip** — missing EOCD record. The sandbox cannot regenerate it (UWP `mv` failure). User-side WSL build produces a working 164 KB archive (verified empirically by Kevin earlier this session); the corrupted file in the repo was a stale sandbox attempt.

2. **README and CHANGELOG said "Phase 7 of 8 complete; Phase 8 pending"** — stale post-Phase-7. Counts also stale ("Eleven ADRs, fourteen invariants" vs reality 19/24/13).

3. **Phase 8 work itself uncommitted** — git HEAD was at `phase-07:` commit, all Phase 8 changes were in the working tree, untracked.

4. **INV-002 (clean-install verification) never executed** — Phase 7 reviewer R6 explicitly committed Phase 8 to running this; the consolidated handoff Open-Question-#1 reaffirmed it; tokenizing as `manual:` is cosmetic relabeling, not the empirical verification ADR 0002 actually requires.

### Secondary concerns identified

5. **Transferability test was synthetic** — agent played both sides, re-introducing the circular validation Mechanism 3 was designed to break. Acknowledged via DEBT-013; reviewer says ship-claim must downgrade to honest "synthetic" framing in release notes.
6. **DEBT-007 closure cosmetic** — INV-002/009/010/011 tokenized as `manual:` but underlying verification is still procedural-human-self-check. Acknowledged.
7. **Spec §4 + §7 stale** — Phase 6 R13 inventory drift not fully addressed. §4 absorbed-skills table missing two companion files (DEBT-010); §7 source-tree listing missing post-spec additions.
8. **DEBT-009 — `executing-plans/SKILL.md:78` cites literal `0008-per-project-involvement.md`** — user projects won't have that exact filename; ungraceful first-day error.
9. **DEBT-011 — broken PreToolUse session-touched-directory heuristic** — fires on every Edit because `git log --reverse` returns oldest commit, training users to ignore the hook.
10. **INV-021 / ADR 0016 cite §3.5 but layout is in §3.6** — documentation lies about which section the invariant protects.
11. **INV-016 currently passes only because Phase 8 has zero commits** — must remain at 1 commit after Phase 8 lands; easy verify.

### Smaller items deferred to v0.1.1 explicitly

- Native-Linux maintainer build not verified (only WSL).
- INV-008 verification still grep-targets a Compass-build-specific filename.
- INV-002 lacks an executable automated-proxy verification line.
- DEBT-012 GitHub URL hardcodes `itecob/socratic-compass` location.

## Main session's response

**R1 (corrupted dist artifact) — User-action required.** Kevin's earlier WSL build produced a working 164 KB archive (confirmed: `[compass] Built ... Size: 164K` plus top-20 contents listing showing 23 SKILL.md). The corrupted sandbox-side file is gitignored (`dist/`) and will not ship. The user must run `Remove-Item dist\* -Recurse -Force` and re-run `bash scripts/package-cowork.sh` in WSL before tagging, then verify `unzip -tq dist/compass.plugin` exits 0. Instructions in the Phase 8 commit message.

**R2 (stale README/CHANGELOG) — Fixed now.** README rewritten: status now "v0.1.0 — Phase 8 of 8 complete"; status table reflects all phases complete; correct counts (19 ADRs, 24 invariants, 13 debt entries); "Known limitations for v0.1.0" section explicitly lists DEBT-002/005/011/012/013. CHANGELOG: Phase 8 entry added with all the work; `## [0.1.0] — 2026-06-25` release section added below with ship-gate summary and known limitations.

**R3 (Phase 8 uncommitted) — Will commit at end of this fix cycle.** Commit instructions in the response section.

**R4 (INV-002 never executed) — User-action required.** Reviewer is correct: tokenizing as `manual:` does not substitute for actual execution. INV-002 must be empirically verified before tagging v0.1.0:
- User installs `dist/compass-claude-code/` into a Claude Code instance with no Superpowers plugin present.
- User invokes `compass:using-compass`.
- Result captured in `.architecture/validation/inv-002-clean-install-2026-06-25.md`.
This is documented as a tag-gating step in the commit message. The alternative (waive via ADR) was rejected because ADR 0002 is the foundational design decision; not verifying it before shipping makes the central claim unsupported.

**R5 (synthetic transferability) — Acknowledged in release notes.** README "Known limitations" section explicitly flags it. CHANGELOG 0.1.0 release section repeats. DEBT-013 logged for v0.1.1 real-user re-run. Reviewer's framing accepted: INV-014 was *exercised mechanically*, not *satisfied per ADR 0011's intent*.

**R6 (DEBT-007 cosmetic) — Acknowledged.** The label change makes `compass:invariant-scan` report "4 MANUAL" honestly rather than skipping malformed entries. The underlying procedural-self-check nature of those four invariants is a real limitation; flagged in release notes; v0.1.1 candidate.

**R7 (spec §4/§7 stale) — Partially fixed.** Spec §4 absorbed-skills table now lists `spec-document-reviewer-prompt.md` and `plan-document-reviewer-prompt.md` (DEBT-010 resolved). §7 source-tree listing not yet updated; deferred to v0.1.1 since the impact is documentation-only and INV-021 / INV-022 are satisfied by the §3.3 + §3.6 rewrites that did happen.

**R8 (DEBT-009 line-78 literal filename) — Fixed now.** `skills/executing-plans/SKILL.md:78` rewritten to handle missing-ADR case gracefully ("typically named ... but may use a different ADR number or filename in projects that didn't"). DEBT-009 marked Resolved.

**R9 (DEBT-011 broken heuristic) — Fixed now via disable.** `hooks/pre-tool-use-edit.sh` and `hooks/pre-tool-use-write.sh` have the session-touched-directory block replaced with an inline comment explaining the DEBT-011 issue and the v0.1.1 fix path. Hooks still exit 0 (INV-003 preserved). The broken-on-every-Edit behavior is removed; the useful work (debt-log surfacing, design-notes existence check) is preserved.

**R10 (ADR 0016 / INV-021 cite §3.5) — Fixed now.** ADR 0016 and INV-021 heading text updated from §3.5 to §3.6 throughout. The original ADR was off-by-one; the layout has always lived in §3.6.

**R11 (INV-016 passes with 0 Phase-8 commits) — Will verify after Phase 8 commit lands.** The Phase 8 commit will use the `phase-08:` prefix; verification command counts up to 1 per prefix; check holds.

## Status after response

HOLD → **SHIP-CONDITIONAL** on two user-side actions before tagging:
1. Run `bash scripts/package-cowork.sh` from WSL with a clean `dist/` to produce a verified-good archive.
2. Run INV-002 clean-install verification, capturing the result in `.architecture/validation/inv-002-clean-install-2026-06-25.md`.

Files this Phase 8 commit includes:
- `.architecture/invariants.md` — 6 verification rewrites + 4 manual tokenizations + section-number fix
- `.architecture/decisions/0016-spec-amendments-and-design-archeology-output-naming.md` — §3.5 → §3.6
- `.architecture/debt-log.md` — DEBT-008/009/010 marked Resolved; DEBT-013 added
- `.architecture/validation/transferability-2026-06-25.md` — new (INV-014 result, synthetic)
- `.architecture/validation/phase-08-2026-06-25-1530.md` — this file
- `specs/2026-06-24-compass-design.md` — §3.3 / §3.6 / §4 rewrites
- `skills/{adversarial-review,architecture-journal,complexity-budget,design-archeology,premise-check,socratic-interview}/SKILL.md` — DEBT-012 citation cleanups
- `skills/executing-plans/SKILL.md` — DEBT-009 fix
- `hooks/pre-tool-use-edit.sh`, `hooks/pre-tool-use-write.sh` — DEBT-011 broken branch disabled
- `README.md` — Phase 8 complete + correct counts + Known Limitations
- `CHANGELOG.md` — Phase 8 entry + [0.1.0] release section

After Phase 8 commit lands AND INV-002 result is captured AND `unzip -tq` on a fresh build passes, tag `v0.1.0`.
