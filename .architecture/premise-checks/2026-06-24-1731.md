# Premise-Check — 2026-06-24 17:31 (back-filled per ADR 0014)

**Source interview:** `.architecture/interviews/2026-06-24-1717.md`
**Task:** First execution session against the Compass plugin implementation plan — "let's build the plugin"

## 1. Two alternative framings

- **Framing A:** Execute the implementation plan to produce the Compass plugin, with the human (Kevin) learning strategic-programming as a side effect of the build.
- **Framing B:** Use the Compass plugin's own discipline (interview-driven, scope-bounded, mutable-involvement) to build itself — a meta-validation that the methodology works before shipping it. (Kevin chose B as primary; A as beneficial side effect.)

## 2. Load-bearing assumptions

1. The implementation plan as written is structurally sound for execution (with the Phase 0 reordering issue noted as DEBT-004 and SD-001).
2. The human wants to remain in the loop at per-skill granularity — not per-task, not single-batch.
3. Cowork's write access to the local repo folder is sufficient to eliminate file-transfer friction. (Proof: this session.)
4. The hybrid involvement setting is correct for this project. Mutable per ADR 0008 if the work calls for adjustment.
5. The scope discipline (ADR 0009) will catch creep without killing valid insights.

## 3. Pivotal question

"Are the 14 absorbed Superpowers skills sufficient quality to absorb verbatim, or will the per-skill light review reveal needed adjustments?" The answer materially changes the cost of Phase 4. We'll learn as we encounter each one.

## 4. Validation against the architecture journal

**Plausibility:** Premise is consistent with all 10 ADRs at the time (0001–0010). No conflicts found.
**Surface feasibility:** Build is feasible — the Cowork-write-access mechanism is proven by this session. One risk: subagent dispatch for `adversarial-review` may not have a Cowork-side equivalent; needs verification in Phase 3 when we get to that skill.
**Prior art:** No prior attempt. This is the first execution session (repo was created today). Nothing to compare against.

## 5. Categorization

**Domain familiarity:** novel — no prior ADRs in this codebase, no established patterns, first session.
**Task type:** feature (building a plugin from a planning document).
**Suggested process weight:** heavy — novel domain, multi-subsystem, decisions about plugin structure are not freely reversible.

The suggested weight is "heavy," but the human's involvement setting (ADR 0008, hybrid per-skill) overrides this. Heavy would normally mean `adversarial-review` runs at every plan iteration; the hybrid setting interprets that as a per-skill question rather than a per-plan one.

## Human's response

Accepted all five outputs. Specifically asked about meta-validation methodology, which led to ADR 0011 being written before any Phase 1 work started. Framings were accurate to intent. Load-bearing assumptions confirmed. Pivotal question retained as a real open question to revisit in Phase 4.
