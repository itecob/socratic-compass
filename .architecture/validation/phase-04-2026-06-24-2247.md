# Phase 4 Validation — 2026-06-24 22:47

**Phase:** 4 (Absorbed Superpowers Skills — Tasks 18–31 + 31a)
**Reviewer:** Claude `general-purpose` subagent, dispatched per ADR 0011 Mechanism 2
**Subagent ID:** a49209b947f5e10d8

## Subagent's inputs

- `specs/2026-06-24-compass-design.md` §4
- `plans/2026-06-24-compass-plan.md` Phase 4
- ADRs 0005, 0017, 0018
- `.architecture/invariants.md` (INV-001, INV-005, INV-023, INV-024)
- All 23 SKILL.md files + companion files

## Subagent's evaluation

**Verdict: FAIL.**

The reviewer caught a real defect that the original rewrite missed: **the `rewrite_file()` function only replaced `superpowers:` (colon-prefixed namespace) and `Superpowers` (capitalized proper noun). It missed lowercase `superpowers` appearing as a path component** (e.g., `docs/superpowers/plans/`, `~/.config/superpowers/worktrees/`, `.superpowers/brainstorm/`).

This is not cosmetic. It changes load-bearing paths:

- `writing-plans/SKILL.md` lines 18 + 138: instructed users to save plans to `docs/superpowers/plans/` — the canonical plan-storage path.
- `brainstorming/SKILL.md` lines 29 + 111: same issue for spec documents at `docs/superpowers/specs/`.
- `finishing-a-development-branch/SKILL.md` lines 183, 227: cleanup ownership check hardcoded `~/.config/superpowers/worktrees/`.
- `using-git-worktrees/SKILL.md` lines 79, 97, 106: legacy fallback at `~/.config/superpowers/worktrees/`.
- 7 files total with 19 unrewritten occurrences.

The reviewer correctly noted that INV-001's verification was constructed to pass even when the spirit failed — the grep pattern only caught the colon-form, not the path component.

### Other concerns raised

- Two undocumented companion files were absorbed beyond spec §4 (`brainstorming/spec-document-reviewer-prompt.md`, `writing-plans/plan-document-reviewer-prompt.md`).
- ADR 0008 is not in the bootstrap template; coupling sections in `executing-plans` and `subagent-driven-development` reference it as if it would exist in user projects.
- adversarial-review SKILL.md should write to `.architecture/validation/` (the coupling expects this); flagged for Phase 5/8 confirmation.

### What the subagent flagged as actually right

- All 14 absorbed skills present with correct frontmatter names.
- `using-compass` rename complete and consistent (frontmatter + self-references + attribution citing upstream `using-superpowers`).
- Attribution footers present on all 14; coupled-skill footers explicitly note the body has been modified beyond standard rewrite.
- 6 of 6 coupling sections internally consistent with ADR 0018's table.
- 8 verbatim-absorbed skills untouched beyond namespace+prose rewrite + attribution.
- Cross-references resolve cleanly.
- All spec-enumerated companion files present.
- Coupling sections correctly handshake with new skills' persistence outputs.
- Workflow chain (using-compass → architecture-journal → socratic-interview → premise-check → design-archeology → brainstorming → tradeoff-matrix → writing-plans → adversarial-review → execute → finish → session-handoff) is structurally sound; the only break was the `docs/superpowers/plans/` path defect.

## Main session's response

**R1 — Fix the rewrite (DONE).** Applied a third Python pattern matching `\bsuperpowers\b` (word boundary, lowercase) → `compass`, excluding lines that contain "Source attribution" or "anthropic-experimental/superpowers" (the upstream URL fragment). Re-ran on the 7 affected files: 19 replacements applied. Verified: `writing-plans/SKILL.md` now says `docs/compass/plans/`, `using-git-worktrees/SKILL.md` now says `~/.config/compass/worktrees/`, etc.

**R2 — Tighten INV-001 verification (DONE).** Verification command updated to `! grep -rEn '\bsuperpowers\b' skills/ 2>/dev/null | grep -vF 'Source attribution' | grep -vF 'anthropic-experimental/superpowers'`. Now catches both colon-form and path-component references. Excludes attribution-footer citations (intentional per ADR 0005) and the upstream GitHub URL fragment. The verification no longer masks the spirit of the invariant.

**R3 — Document extra companion files (DONE).** DEBT-010 logged. Files retained (harmless after rewrite); spec §4 update deferred to Phase 8 per the existing INV-021 spec-rewrite pattern.

**R4 — adversarial-review writes to .architecture/validation/ (CONFIRMED).** Verified by reading `skills/adversarial-review/SKILL.md` — its Process step 5 says "Save to `.architecture/validation/phase-NN-YYYY-MM-DD-HHMM.md`." Confirmed.

**R5 — ADR 0008 not in bootstrap (DONE).** DEBT-009 logged. Fix is small (write a template-stub example into `templates/architecture/decisions/0008-involvement-setting-example.md` or update coupling sections to reference an abstract ADR); deferred so this commit doesn't bloat.

**R6 — Do not advance to Phase 5 until items 1 and 2 are fixed (RESPECTED).** Both fixed in this same commit cycle. Phase 4 commit will include the post-fix state.

## Status after response

FAIL → **Pass** conditional on the Phase 4 commit landing with the post-fix artifacts.

Files this commit includes:
- 14 absorbed SKILL.md files in `skills/` (8 verbatim + 6 coupled), all with the third rewrite pattern applied
- Companion files for each absorbed skill (16 files total)
- New ADRs 0017, 0018 in `.architecture/decisions/`
- Updated `.architecture/invariants.md` (INV-023, INV-024 added; INV-001 verification tightened)
- Updated `.architecture/manifest.md` (ADRs 0017, 0018 indexed)
- Updated `.architecture/debt-log.md` (DEBT-009, DEBT-010 added)
- This validation file
- Updated `README.md` and `CHANGELOG.md` for Phase 4 complete

**Proceeding to Phase 5** once Phase 4's consolidated commit lands. Phase 5 builds the optional Claude Code hooks per ADR 0017's "hooks are reinforcement, not source" position.
