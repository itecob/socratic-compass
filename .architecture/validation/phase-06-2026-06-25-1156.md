# Phase 6 Validation — 2026-06-25 11:56

**Phase:** 6 (Packaging — Tasks 36–37)
**Reviewer:** Claude `general-purpose` subagent, dispatched per ADR 0011 Mechanism 2
**Subagent ID:** a59f72f819960b69f

## Subagent's inputs

- `specs/2026-06-24-compass-design.md` §7 (Packaging)
- `plans/2026-06-24-compass-plan.md` lines 1775–1851 (Tasks 36, 37)
- `scripts/package-cowork.sh`, `scripts/package-claude-code.sh` (initial drafts)
- `LICENSE` (initial draft)
- `.architecture/invariants.md` (INV-001)
- `.architecture/debt-log.md` (DEBT-005)
- Smoke-test results (both scripts exit 0; both idempotent; exclusions verified)
- Option-B exclude decision context (Phase 6 interview override of session's option-C recommendation)

## Subagent's evaluation

**Verdict: FAIL.**

Reviewer caught 14 concerns; 8 load-bearing, 6 cosmetic.

### Load-bearing concerns (must fix)

1. **README.md truncated mid-bullet at line 38** (`- \`arc`). Shipped to both targets. Phase status table still showed Phase 6 as Pending.
2. **CHANGELOG.md had no Phase 6 entry.** Phase 6 still listed under "Still to come."
3. **No ADR for the expanded exclude list.** Per ADR 0012, plan deviations require an ADR. The option-B decision was recorded only in script header comments and the interview, not in `.architecture/decisions/`.
4. **`set -euo pipefail` silently downgraded to `set -u`.** Plan specified strict mode; scripts shipped with `set -u` only. Material consequence in `package-claude-code.sh`: when `rm -rf` partially fails (UWP-mounted folder), script reports success with stale-file-leak hazard.
5. **`package-claude-code.sh` not idempotent across structural changes.** `rsync -a` without `--delete` leaves stale files when sources are renamed or deleted upstream. Smoke test's "idempotent" claim verified only content match, not structural deletion.
6. **Orphan temp file `dist/ziitB0W1`** (162 KB) from a previous failed run polluted the user's `dist/` directory. Trap covered the source-side `/tmp` temp, not the destination-side leftover from UWP rename-failure.
7. **Zip entries all `rwx------` (0700).** Sandbox umask 077 carried into zip stored attributes. Multi-user installs broken after unzip; Markdown files needlessly +x.
8. **Cowork `.plugin` format risk uncovered.** DEBT-005 covered Claude Code manifest schema only; same risk applies to whether Cowork accepts a renamed-zip as `.plugin` install format.

### Cosmetic concerns (acknowledge / defer)

9. Skill citations point to `.architecture/` paths now stripped from the install (e.g., `socratic-interview/SKILL.md:151` cites the first interview transcript).
10. `rsync` unavailable on Windows-native Git Bash without optional Unix tools or WSL.
11. *(see #8 — same item, the reviewer numbered them differently)*
12. LICENSE attribution paragraph URL inconsistency — LICENSE had `obra/superpowers`, skill footers have `anthropic-experimental/superpowers`. LICENSE was wrong; skill footers are the canonical absorbed-from URL.
13. Spec §7 `templates/architecture/` inventory drift — the shipped template includes directories added by ADRs 0013–0016 that §7 doesn't list. INV-021 covers spec rewrite at Phase 8.
14. Build scripts shipped inside the package they build — harmless under spec §7's "source tree" framing.

### What the subagent flagged as actually right

- INV-001 (`superpowers:` exclusion) holds in the shipped package. Only matches are in attribution footers (intentional per ADR 0005) and CHANGELOG/spec/plan documentation.
- All 23 SKILL.md present in both targets.
- All 4 hooks present in both targets.
- Exclusion logic itself is sound — `.architecture/`, `specs/`, `plans/` truly absent from output.

## Main session's response

**R1 (README truncation + Phase 6 status) — Fixed now.** Completed the truncated bullet list (lines 38+ now describe architecture-journal, session-handoff, invariant-scan, complexity-budget, plus the absorbed-skills paragraph and a new "Building the package" section). Phase 6 row in the status table now shows Complete; header now reads "Phase 6 of 8 complete — Packaging shipped."

**R2 (CHANGELOG Phase 6 entry) — Fixed now.** New `### Added (Phase 6 — Packaging, 2026-06-25)` section before the "Still to come" list. Removes Phase 6 from "Still to come."

**R3 (no ADR for option-B exclude) — Fixed now.** ADR 0019 written. Documents the option-A/B/C trichotomy, the human's override of the main session's option-C recommendation, the rationale, the consequences (including DEBT-012 for in-install citation breakage), and the related strict-mode + umask choices. Manifest.md indexes ADR 0019.

**R4 (`set -u` downgrade) — Fixed now.** Both packaging scripts restored to `set -euo pipefail`. The Phase 5 hooks remain on `set -u` only because INV-003 requires hooks to exit 0; packaging scripts have no such constraint and should fail loud. ADR 0019 documents this differentiation explicitly.

**R5 (no `--delete` in rsync) — Fixed now.** `package-claude-code.sh` now uses `rsync -a --delete`. Stale files from prior builds are removed when their source is removed upstream.

**R6 (orphan `dist/ziitB0W1`) — Partially fixed.** Rewritten `package-cowork.sh` stages files in a tmpdir, builds the zip in `$TMPDIR`, then `mv -f`s into `dist/`. This eliminates zip's "create temp + rename in place" pattern in `dist/`, which was the source of the orphan. The pre-existing `ziitB0W1` file (162 KB, from the original script's UWP rename-failure) cannot be removed sandbox-side; user-side `Remove-Item dist\*` in PowerShell will clean it. Documented in CHANGELOG. `dist/` is gitignored so the orphan will not be committed.

### Sandbox vs user-side verification

Verifying the rewritten scripts revealed a sandbox-specific limitation that does not affect user-side builds. On the Windows-mounted (UWP/virtiofs) `dist/` directory:

- `mv -f /tmp/compass-XXXXXX.plugin dist/compass.plugin` fails with "unable to remove target: Operation not permitted" because the sandbox can replace neither the existing target nor an orphan with UWP-restricted ACLs.
- `rsync --delete` cannot unlink a stale file created by the sandbox under UWP rules.

Both failures are restricted to the Cowork session sandbox and do not affect the user's PowerShell-side build environment, where `dist/` is a regular Windows path under their user account.

End-to-end script LOGIC was verified by running the equivalent of the scripts manually with the destination redirected to `/tmp/compass-verify.plugin`. Results:

- Stage tree built correctly via `tar` with all excludes.
- All `*.sh` files in stage: mode 755.
- All regular files in stage: mode 644.
- All directories in stage: mode 755.
- Zip entries inside the archive: 644/755 (the original 0700 defect — concern 7 — is resolved).
- Excludes: zero entries from `.architecture/`, `specs/`, `plans/`, `.gitignore`.
- Includes: 23 SKILL.md, 4 hook scripts, `plugin.json`, `LICENSE`, `NOTICE`, `README.md`, `CHANGELOG.md`, full `templates/architecture/` and `scripts/`.
- INV-001 holds on the shipped archive (0 bad `superpowers:` references after excluding attribution-footer citations and the upstream URL fragment).
- `NOTICE` URL matches all skill footers (`anthropic-experimental/superpowers`).

The user-side smoke test is the canonical verification for Phase 6's deliverables.

**R7 (zip permissions 0700) — Fixed now.** Both scripts set `umask 022` at top, then explicit `find ... -exec chmod {644,755} {}` on the staged tree before zipping or rsyncing. Shipped entries will now be world-readable with scripts world-executable.

**R8 (Cowork `.plugin` format uncovered by DEBT-005) — Fixed now.** DEBT-005 extended to cover both the manifest schema and the `.plugin` archive format. Phase 8 transferability test (INV-014) exercises this implicitly.

**R9 (skill citations point at stripped paths) — Logged as DEBT-012.** Rewrite is mechanical (sed pass adding the GitHub URL prefix to `.architecture/` references) but touches multiple files. Deferred to Phase 8 alongside the spec §3.5 / §7 rewrite per INV-021.

**R10 (rsync on Windows native) — Documented.** `package-claude-code.sh`'s missing-rsync error now mentions Git for Windows + optional Unix tools or WSL. README's new "Building the package" section also notes the requirement.

**R11 (= same as R8) — N/A.**

**R12 (LICENSE URL inconsistency) — Fixed now.** LICENSE is now pure MIT (no attribution paragraph). New `NOTICE` file carries the upstream attribution with the canonical `https://github.com/anthropic-experimental/superpowers` URL, matching all skill footers. Both LICENSE and NOTICE are in the shipped package (NOTICE added to both excludes' "include" set).

**R13 (spec §7 template inventory drift) — Acknowledged; deferred.** Already covered by INV-021's Phase 8 spec rewrite. No separate action needed.

**R14 (build scripts ship inside the package) — Deliberate.** Spec §7's source-tree framing makes shipping the scripts defensible. No change.

## Status after response

FAIL → **Pass** conditional on the Phase 6 commit landing with the post-fix artifacts.

Files this commit includes:
- 2 rewritten packaging scripts (`scripts/package-cowork.sh`, `scripts/package-claude-code.sh`) with `set -euo pipefail`, `umask 022`, staging+chmod normalization, `mv -f`/`--delete` correctness fixes
- `LICENSE` (pure MIT)
- `NOTICE` (upstream attribution with corrected URL)
- New ADR 0019 in `.architecture/decisions/`
- Updated `.architecture/manifest.md` (ADR 0019 indexed)
- Updated `.architecture/debt-log.md` (DEBT-005 extended, DEBT-012 added)
- This validation file
- Updated `README.md` (Phase 6 complete, truncation fixed, build section added)
- Updated `CHANGELOG.md` (Phase 6 entry)

**Proceeding to Phase 7** once Phase 6's consolidated commit lands. Phase 7 is the dogfood reconcile — the plugin's own `.architecture/` was built in Phase 0 ahead of plan order (per DEBT-004), so most of Phase 7 (Tasks 38–42) is already done. The remaining work is to identify and patch any plan-vs-reality gaps.
