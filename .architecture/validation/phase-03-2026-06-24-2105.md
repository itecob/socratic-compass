# Phase 3 Validation — 2026-06-24 21:05

**Phase:** 3 (Nine new strategic skills — Tasks 9a, 10–17)
**Reviewer:** Claude `general-purpose` subagent, dispatched per ADR 0011 Mechanism 2
**Subagent ID:** a57065e53461b8069

## Subagent's inputs

Per ADR 0011, the subagent received:
- `specs/2026-06-24-compass-design.md` §3.1–§3.9 (the nine skills' specs)
- `plans/2026-06-24-compass-plan.md` Phase 3 (Tasks 9a, 10–17)
- `.architecture/decisions/0008, 0010, 0014, 0015` (ADRs that directly affected Phase 3)
- `.architecture/invariants.md` and `conventions.md`
- All nine SKILL.md files
- New ADRs 0014 and 0015
- Back-filled premise-check artifact
- Template README updates

The subagent was told to flag any inconsistency introduced by the post-spec ADRs (0011, 0013, 0014, 0015) and not to flag the absent per-task commits (per ADR 0012).

## Subagent's evaluation (verdict and key findings)

**Verdict: Pass with concerns.**

### Concerns raised

1. **Spec never amended for ADRs 0011/0013/0014/0015.** Four post-spec additions to `.architecture/` schema (`scope-deferred.md`, `validation/`, `premise-checks/`, `design-notes/`) are not in spec §3.5. Spec is the contract; contract diverged silently.
2. **`design-archeology` renamed output #4** from spec §3.3's "Existing Design Notes" block to "Implications for the Proposed Change." Functional improvement, but silent spec divergence.
3. **Three frontmatter descriptions violate `writing-skills` no-workflow-summary rule** (`design-archeology`, `adversarial-review`, `architecture-journal`). The plugin's own skills violate the discipline the plugin promotes.
4. **Stale `.bak` files** in `.architecture/manifest.md.bak`, `scripts/bootstrap-architecture.sh.bak`, `templates/architecture/manifest.md.bak` from sandbox sed runs. Untracked but unsightly.
5. **`debt-log.md` had a corruption** (`ct).` fragment from earlier UWP truncation). Cleaned up.
6. **`socratic-interview` Process step 2** mandated a scope statement ("The plan says this task exists to do X") that doesn't make sense when the interview IS the first pre-planning step (no plan exists yet). Misapplied ADR 0009.
7. **`session-handoff` SKILL.md invents "backseating detection" terminology** not in ADR 0008 itself (ADR 0008 supports it but doesn't name it).
8. **`architecture-journal` description** lists nine directory categories — far too long, violates writing-skills.
9. **INV-002, INV-007, INV-008** are unverifiable as written (prose "Manual:" not the `manual` token; INV-007 has no verification at all; INV-008 verification is Compass-build-specific).
10. **Persistence path patterns are inconsistent** across `.architecture/` subdirectories (date-stamps vs source-mirrors vs typed prefixes). Defensible per individual ADR but no convention captures the system.
11. **`architecture-journal` session-start "load only relevant"** rule is under-specified for the new directories — "recent" and "planning cycle in progress" are not defined.
12. **Phase 4 coupling:** absorbed `brainstorming` will need to be modified or wrapped to read `.architecture/premise-checks/` and `.architecture/design-notes/` (with SHA freshness check). Flag for Phase 4 entry conditions.

### What the subagent flagged as actually right

- All nine SKILL.md files exist with correct structure.
- `socratic-interview` is the model meta-skill; Iron Rules and Process map to spec §3.1; persistence refusal is explicit.
- `premise-check` is the most-extended skill and the extension is well-executed (iron law updated, persistence wired in, rationalization table covers the skip-persistence rationalization).
- `adversarial-review` correctly implements the meta-validation; subagent prompt template embeds the discipline; four-way disposition of findings matches actual usage.
- `compass:` cross-references resolve correctly; lowercase, hyphenated, REQUIRED SUB-SKILL marker per C-002.
- Back-fill of `2026-06-24-1731.md` premise-check per ADR 0014 is correct.
- Template READMEs for new directories exist and reference Compass skills.
- `architecture-journal` correctly delegates session-end to `session-handoff` and shortcut-logging to `complexity-budget` (single source of truth).

## Main session's response

**R1 (spec amendment, design-archeology naming) — Handled via ADR 0016.** Single combined ADR covers both. Per ADR 0016, spec rewrite deferred to Phase 8 (per INV-021/022); spec file itself remains stable until then (per INV-013's spec-stability principle). `design-archeology` SKILL.md gained a wrapper note explicitly stating the four outputs constitute the "Existing Design Notes" block from spec §3.3.

**R3 (three description violations) — Fixed now.** Descriptions tightened to symptom-only:
- `design-archeology`: "Use when about to modify a file or subsystem you have not authored or recently touched"
- `adversarial-review`: "Use after a plan is drafted but before execution begins, or at the end of each phase in a multi-phase build"
- `architecture-journal`: "Use at session start in a project with a .architecture/ directory, and when a structural decision is being made or an invariant is established"
- `premise-check`: also tightened ("Use after compass:socratic-interview has produced a transcript"), since it had a milder version of the same violation.

**R4 (stale `.bak` files) — Logged as DEBT-008.** Sandbox rm fails with "Operation not permitted." User clears in PowerShell during commit prep.

**R5 (debt-log corruption) — Fixed now.** Truncated `ct).` fragment removed; file ends cleanly at DEBT-006's `**Logged:**` line.

**R6 (socratic-interview scope step misapplication) — Fixed now.** Step 2 now reads as conditional: if a prior plan exists, state scope; if the interview IS the first pre-planning step, skip the scope statement (the interview defines scope).

**R7 (`session-handoff` "backseating detection" terminology) — Acknowledged, no action.** The mechanism is real and supported by ADR 0008's mutability clause. The SKILL.md is the right place to operationalize it. Naming the mechanism in the SKILL.md rather than retroactively in the ADR is acceptable.

**R8 (architecture-journal description length) — Addressed by R3.** New description is short and symptom-focused.

**R9 (INV-002, INV-007, INV-008 unverifiable) — Logged as DEBT-007.** Three small edits required; deferred so this commit doesn't pull in pre-existing cleanup work. Will fix in Phase 5 or 8.

**R10 (path pattern inconsistency, missing C-009) — Acknowledged.** ADR 0016 references the pattern divergence; a follow-up convention can be written when more skills exist to inform the rule. Not blocking.

**R11 (architecture-journal recency under-specification) — Deferred.** Defining "recent" and "planning cycle in progress" requires more usage data. Will refine when Phase 5 hooks reveal the operational pattern.

**R12 (Phase 4 brainstorming coupling) — Acknowledged; flag for Phase 4 entry conditions.** When Phase 4 begins, before absorbing brainstorming, the main session must decide: wrap the absorbed skill, post-process the absorbed SKILL.md to inject premise-check / design-notes reads, or document an extension mechanism. This is the meaningful Phase 4 design decision.

## Status after response

Pass with concerns → **Pass** conditional on:
1. Phase 3 commit landing (handled in user-side PowerShell session).
2. User deleting the three `.bak` files in PowerShell before staging (DEBT-008 closes when done).

Files this commit includes:
- 9 new SKILL.md files in `skills/`
- New ADRs 0014, 0015, 0016 in `.architecture/decisions/`
- Updated `.architecture/invariants.md` (added INV-019, INV-020, INV-021, INV-022)
- Updated `.architecture/debt-log.md` (added DEBT-007, DEBT-008; cleaned up DEBT-006 fragment)
- Updated `.architecture/manifest.md` (indexed ADRs 0014, 0015, 0016; added premise-checks/, design-notes/ to Contents)
- New `.architecture/premise-checks/2026-06-24-1731.md` (back-filled)
- New template files (`templates/architecture/premise-checks/README.md`, `templates/architecture/design-notes/README.md`)
- Updated `templates/architecture/manifest.md` (premise-checks/, design-notes/)
- Updated `scripts/bootstrap-architecture.sh` (premise-checks/, design-notes/ in Contents output)
- Updated `skills/socratic-interview/SKILL.md` (scope step fix)
- Updated `skills/design-archeology/SKILL.md` (Existing Design Notes wrapper note)
- Updated `skills/{design-archeology,adversarial-review,architecture-journal,premise-check}/SKILL.md` (tightened descriptions)
- This validation file

**Proceeding to Phase 4** once Phase 3's consolidated commit lands. Phase 4 entry conditions will explicitly include the brainstorming-coupling decision (R12).
